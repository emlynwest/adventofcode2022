#!/usr/bin/env python
from setuptools import setup

install_requires = [
]

setup(
    name="adventofcode2022",
    author="Emlyn West",
    install_requires=install_requires,
    python_requires=">=3.9",
)
